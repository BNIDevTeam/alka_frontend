import Link from "next/link";
import { auth } from "@/auth";
import { getDashboardData } from "@/lib/api/admin";
import { Target, Users, Gift, CreditCard, ArrowRight, BarChart3 } from "lucide-react";
export default async function AdminDashboardPage() { const session = await auth(); const dashboard = await getDashboardData(); const stats = [{ title: "Campaigns", value: dashboard.summary.totalCampaigns, icon: Target, href: "/admin/campaigns" }, { title: "Contacts", value: dashboard.summary.totalContacts, icon: Users, href: "/admin/dashboard" }, { title: "Volunteers", value: dashboard.summary.totalVolunteers, icon: Users, href: "/admin/dashboard" }, { title: "Donations", value: dashboard.summary.totalDonations, icon: Gift, href: "/admin/dashboard" }, { title: "Donation Amount", value: `₹ ${dashboard.summary.totalDonationAmount.toLocaleString()}`, icon: CreditCard, href: "/admin/dashboard" }]; return <div className="space-y-8"><div className="rounded-2xl bg-gradient-to-r from-blue-600 via-blue-700 to-indigo-800 px-6 py-5 text-white shadow-xl"><h1 className="text-2xl font-bold">Welcome back, {session?.user?.name || 'Admin'}</h1><p className="mt-1 text-sm text-blue-100">Your ALKA website and admin panel now run from the same Next.js app.</p></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-5">{stats.map(({ title, value, icon: Icon, href }) => <Link key={title} href={href} className="group rounded-2xl border border-gray-100 bg-white p-5 shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><div className="flex items-center justify-between"><div className="rounded-xl bg-blue-50 p-3 text-blue-600"><Icon className="h-5 w-5" /></div><ArrowRight className="h-4 w-4 text-gray-300 transition group-hover:text-blue-600" /></div><p className="mt-4 text-sm text-gray-500">{title}</p><p className="mt-1 text-2xl font-bold text-gray-900">{value}</p></Link>)}</div><div className="grid gap-6 lg:grid-cols-2"><section className="rounded-2xl border border-gray-100 bg-white p-6 shadow-sm"><div className="mb-4 flex items-center gap-2 text-gray-900"><BarChart3 className="h-5 w-5 text-blue-600" /><h2 className="text-lg font-semibold">Recent donations</h2></div><div className="space-y-3">{dashboard.recentDonations.length ? dashboard.recentDonations.map((item) => <div key={item.id} className="rounded-xl border border-gray-100 px-4 py-3"><div className="flex items-center justify-between gap-3"><p className="font-medium text-gray-900">{item.donorName}</p><span className="text-sm font-semibold text-emerald-600">₹ {item.amount.toLocaleString()}</span></div><p className="mt-1 text-xs text-gray-500">{item.status} • {new Date(item.createdAt).toLocaleDateString('en-IN')}</p></div>) : <p className="text-sm text-gray-500">No recent donations found.</p>}</div></section><section className="rounded-2xl border border-gray-100 bg-white p-6 shadow-sm"><div className="mb-4 flex items-center gap-2 text-gray-900"><Target className="h-5 w-5 text-blue-600" /><h2 className="text-lg font-semibold">Recent campaigns</h2></div><div className="space-y-3"><p className="text-sm text-gray-500">Use the campaigns section to create, edit, publish, and sort website campaigns through the Node API.</p><Link href="/admin/campaigns" className="inline-flex items-center rounded-lg bg-blue-600 px-4 py-2 text-sm font-medium text-white">Open campaigns</Link></div></section></div></div>; }
